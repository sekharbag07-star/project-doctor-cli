# Contributing

1. Create a focused branch.
2. Keep analyzers small and implement `Analyzer`.
3. Add or update unit and integration tests.
4. Run `dart format .`, `dart analyze`, and `dart test`.
5. Open a pull request describing behavior and test coverage.

Avoid adding dependencies unless the standard library cannot provide the required behavior. Keep report output deterministic.
